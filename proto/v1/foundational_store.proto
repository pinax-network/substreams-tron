syntax = "proto3";

package tron.foundational_store.v1;

enum Keys {
  STORE_TYPE_UNSPECIFIED = 0;
  PAIR_CREATED = 1;
}

// key = <1-byte prefix of Keys enum> + <pair bytes>
message PairCreated {
  bytes token0 = 1;
  bytes token1 = 2;
}